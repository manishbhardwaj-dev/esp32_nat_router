- name: Build firmware (NO INSTALL STEP)
  shell: bash
  run: |
    set -e

    echo "Sourcing ESP-IDF..."
    source $IDF_PATH/export.sh

    echo "Cleaning build (safe CI)"
    rm -rf build

    echo "Setting target"
    idf.py set-target esp32

    echo "Building firmware"
    idf.py build